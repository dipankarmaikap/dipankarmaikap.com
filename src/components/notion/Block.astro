---
import Code from "~/components/notion/Code.astro";
import Pragraph from "./Paragraph.astro";
interface Props {
  block: any;
}

const { block } = Astro.props;
---

{block.type === "paragraph" && <Pragraph {block} />}
{block.type === "code" && <Code {block} />}
<h1>block</h1>
